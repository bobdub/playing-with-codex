{
  "name": "playing-with-codex",
  "version": "0.1.0",
  "private": true,
  "description": "Compiler and tooling for the |Ψ imagination syntax.",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/sync-homepage.js",
    "sync:homepage": "node scripts/sync-homepage.js",
    "check": "npm run build && node dist/index.js docs --format summary && node scripts/check-symbol-filter.js && node scripts/check-ethics.js",
    "build:index": "npm run build && node dist/index.js docs --format index --pretty --out docs/knowledge-index.json"
  },
  "bin": {
    "psi-compiler": "dist/index.js"
  },
  "keywords": [
    "psi",
    "dsl",
    "compiler"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.0"
  }
}
